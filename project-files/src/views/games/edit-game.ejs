<%- include('../partials/header') %>
    <form method="POST" action="/my-games/edit-game/<%= game.id %>" class="edit-game-form">
        
        <h1>Edit Game</h1>

        <!-- Title field -->
        <div class="form-item">
            <label for="title">Title</label>
            <input
                type="text"
                id="title"
                name="title"
                required
                placeholder="Jack and Jill's Gender Reveal"
                value="<%= game.title %>"
            >
        </div>

        <!-- Gender field -->
        <div class="form-item">
            <label for="gender">Gender</label>
            <select
                id="gender"
                name="gender"
                required
            >
                <option
                    value="NONE"
                    <%= (game.gender && game.gender.toUpperCase() === 'NONE') ? 'selected' : '' %>
                >
                    NONE
                </option>
                <option
                    value="BOY"
                    <%= (game.gender && game.gender.toUpperCase() === 'BOY') ? 'selected' : '' %>
                >
                    BOY
                </option>
                <option
                    value="GIRL"
                    <%= (game.gender && game.gender.toUpperCase() === 'GIRL') ? 'selected' : '' %>
                >
                    GIRL
                </option>
            </select>
        </div>

        <button type="submit">Save Changes</button>
        <a href="/my-games/about-game/<%= game.id %>" class="button">Discard Changes</a>

        <!-- <p>Twins!?!?  Triplets!?!?!?</p>
        <button>Add Another Baby</button> -->

    </form>
<%- include('../partials/footer') %>